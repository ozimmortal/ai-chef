import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'AI Chef - Recipe Generator',
  description: 'Transform your ingredients into delicious, healthy recipes instantly with AI-powered cooking assistance.',
  keywords: 'AI chef, recipe generator, healthy recipes, cooking, ingredients, nutrition',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}